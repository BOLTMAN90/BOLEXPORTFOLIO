# Phase 11 — SEO & Performance `#seo-perf`

## SEO
- [ ] `metadata` in `layout.tsx` (title, description, OG, Twitter)
- [ ] `robots.txt` / `sitemap.ts` (optional v1)
- [ ] Semantic HTML (`main`, `section`, `h1` once)
- [ ] JSON-LD Person schema (optional)

## Performance
- [ ] Dynamic import Three.js bundle
- [ ] `loading.tsx` skeleton
- [ ] `next/image` sizes + priority only on hero if needed
- [ ] Font `display: swap`

## Favicon
- [ ] SVG or ICO in `public/`
- [ ] `app/icon.tsx` or metadata icons

## Theme
- [ ] `next-themes` with smooth CSS transition
- [ ] `defaultTheme="dark"`

## Exit criteria
- Lighthouse performance > 80 mobile (target)
- No hydration warnings in console
